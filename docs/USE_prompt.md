项目路径：
D:\work_floder\jiezhifa\TankEye_source_for_new_pc

请基于当前 TankEye-Iris 继续维护。

开始前必须先读取并遵守：
AGENTS.md
docs/project-memory/OPERATING_LIMITS.md
docs/project-memory/MEMORY.md
docs/project-memory/TASK_HISTORY.md
docs/project-memory/HANDOFF.md
docs/project-memory/AI_ROLE_WORKFLOW.md
docs/project-memory/2026-08-03-slimming-correction.md

不要依赖 README。
项目事实以源码、CMake、配置、脚本、测试和 docs/project-memory 文档为准。

## 先核查再执行
1. 处理任务前，先检查需求和前提是否存在事实错误、逻辑漏洞、信息缺失、过时判断或不合理假设。
2. 发现问题时要直接指出，并说明原因、风险和更合理的方案。
3. 先阅读相关代码、配置、测试和项目规则，再进行修改。
4. 区分四类信息：已从代码或文档确认的事实、基于现有信息的合理推测、尚未验证的假设、无法确认的信息。
5. 不默认认同技术判断，也不要为了反对而反对。
6. 优先遵循项目现有框架、风格和设计，避免无关重构。
7. 修改后运行相关测试、检查或构建；没有验证就明确说明，不要声称已经验证。
8. 无法确认的内容不要编造。
9. 若需求和代码都没有明显问题，就直接执行，不必刻意挑错。

通用工作要求：
1. 只修改当前项目目录内文件，目录外只能读。
2. 系统级、环境级、依赖安装、注册表、驱动、全局配置、真实 PLC、真实相机、真实设备动作相关操作必须先问我。
3. 不要覆盖或回滚我已有改动。
4. 读取和写入仓内文本文件默认使用 UTF-8；PowerShell 读写必须显式使用 `-Encoding UTF8`。
5. 每次修改后必须做真实构建/测试验证，不能用口头模拟结果代替。
6. 每次任务结束后必须更新：
   - docs/project-memory/TASK_HISTORY.md
   - docs/project-memory/MEMORY.md
   必要时更新：
   - docs/project-memory/HANDOFF.md
7. 当前大方向是“真正瘦身优化”：优先合并重复代码、减少项目总代码量和维护点，不要只是把大文件拆成更多文件。
8. 当前协作流程为六角色评审：
   - 产品经理
   - 项目经理
   - 架构师
   - CV 工程师
   - Qt 工程师
   - 测试工程师

代码修改规则：
1. 最小改动原则：只修改我明确指定的功能部分，其他现有代码的逻辑、结构、命名、风格一律保持原样，禁止“顺手优化”或“重构周边代码”。
2. 替换优先：同一功能只能有一份实现。修改时必须先删除旧逻辑，再写入新逻辑。禁止保留注释掉的旧代码、废弃函数或备用方案。
3. 完整输出：必须输出修改后的完整函数或完整文件，禁止只输出片段或 diff。
4. 先删后写：输出新代码前，必须先列出【删除清单】，明确写出删除的函数/变量/import/旧逻辑、行号和理由。
5. 零冗余：修改后必须清理未使用的 import、未调用的函数/变量、注释掉的代码块、功能重复的旧逻辑。
6. 禁止事项：绝对不允许保留注释掉的旧代码；不允许写临时/备用方案并存；不允许在文件末尾追加新函数而不删旧函数；不允许使用 TODO/FIXME/HACK 等标记拖延清理；不允许改动我未提及的现有代码逻辑。
7. 如果我认为删除清单有误，我会告诉你保留哪些。在得到我明确确认前，不要擅自保留“以防万一”的代码。

涉及新功能、高风险 UI/CV/PLC 改动时，先按 docs/project-memory/AI_ROLE_WORKFLOW.md 的六角色流程评审：
- 产品经理
- 项目经理
- 架构师
- CV 工程师
- Qt 工程师
- 测试工程师

开始工作前，请先汇报：
1. 你读到了哪些项目记忆文件；
2. 当前 git 分支和工作区状态；
3. 是否存在未提交或未跟踪文件；
4. 你对当前任务边界的理解。

执行代码修改时，请按以下流程输出：
1. 【需求理解】
2. 【删除清单】
3. 【变更清单】
   必须包含：未改动部分：除上述变更外，其余代码逻辑保持不变。
4. 【完整代码】
   输出完整函数或完整文件，不要只输出片段。
5. 【验证结果】
   写清楚真实执行过的构建/测试/启动命令和结果。
6. 【自检完成】
   - [x] 仅修改了指定功能，未改动其他现有逻辑
   - [x] 已删除所有被替换的旧代码
   - [x] 已清理未使用的 import/变量/函数
   - [x] 无注释掉的废弃代码
   - [x] 同一功能无重复实现
   - [x] 输出为完整上下文，非片段
