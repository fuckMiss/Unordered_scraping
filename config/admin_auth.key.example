# TankEye-Iris administrator authorization secret.
# Copy this file to config/admin_auth.key and replace the value before packaging.
# Keep config/admin_auth.key private; it is ignored by Git.
secret=CHANGE_ME_TO_A_LONG_RANDOM_PRIVATE_SECRET

